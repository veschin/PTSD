issues:
  - id: stage-name-inconsistency
    category: config
    summary: "stage names inconsistent across modules (test/tests, implemented/impl)"
    fix: "use canonical names from CLAUDE.md: prd, seed, bdd, tests, impl"
